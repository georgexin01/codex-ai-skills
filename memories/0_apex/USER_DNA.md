# 🧬 User Taste Profile: "Trusta Industrial" (2026-05-05)
> 🤖 **MODEL SYNC**: This file contains the User's subjective aesthetic preferences. Any AI model working on this project MUST respect these "Taste Fingerprints" above all general design knowledge.

> This document captures the aesthetic "DNA" and personal taste of the User. It serves as the primary guidance for all future "Design Senses" evolution.

## 1. Visual Fingerprint
- **Typographic Gravity**: Prefers **700 (Bold)** as the primary communicative weight. 900 is reserved for "numerical spectacle." Anything below 500 is considered too weak for industrial contexts.
- **Dimensionality**: Loves **Glassmorphism** (backdrop-blur + translucent white) for high-level UI feedback. It adds a "material" layer that feels like professional hardware consoles.
- **Precision Scale**: Strictly favors **6px** for linear elements (progress bars, status lines). This is a "Sleekness Signature" that differentiates professional tools from consumer toys.
- **Chromatic Depth**: High affinity for **Violet/Teal/Dark** gradients with "Glow" highlights. UI must feel "alive" but anchored in dark, tech-focused foundations.
- **High-Contrast Mandate**: Absolute zero-tolerance for "Gray-on-Gray". Contrast must be sharp and human-readable (e.g., Never use light-gray buttons on a gray footer background).

## 2. Interaction DNA
- **Zero-Friction Targets**: Prefer **Clickable Cards** over isolated buttons. The User values "sloppy" interaction ergonomics—where the target area is maximized for speed.
- **Information Culling**: "Focus-First" philosophy. Show the **Current Mission** only. Hide legacy or secondary data behind "View More" or sub-pages.
- **Visual Scannability**: High preference for **Symbolic Icons (SVG)** over text labels for repeatable actions.

## 3. Communication Patterns
- **Direct & Iterative**: The User provides feedback in short, direct bursts (e.g., "more padding top", "center this").
- **Visual Validation**: Relies on screenshots and visual checks.
- **Standardization**: Requests system-wide standardization immediately after a successful pattern is found (e.g., "apply this to both apps").

## 4. Implicit Rules for the AI
1. **Never ask** about progress bar height; it is always 6px.
2. **Never default** to 900 weight for headers; use 700.
3. **Always center** landing UI (Login) unless full-bleed layout is required.
4. **Proactively use glass** for all notification/modal overlays.
5. **Always verify contrast**: Ensure absolute readability. Never pair low-contrast grays (e.g., light-gray text/buttons on gray backgrounds).
6. **GitNexus is allowlisted with one controlled auto-index case** (2026-05-29). Auto-run `gitnexus analyze` once for new large `vben admin` / Supabase admin projects, and re-run after long/structural updates when impact analysis will save time. For websites and apps (PHP sites, static/marketing pages, small PWAs), skip GitNexus and use grep/glob. Never run GitNexus on `.codex`. Full policy: `memories/2_governance/GITNEXUS.md` → "Project allowlist".
7. **Video URL fields are platform-agnostic** (2026-05-20). When building a content module with a video field, never hardcode "YouTube" in labels, placeholders, link text, or URL validation. Use generic `Video URL` / `Enter any video URL`. The user posts TikTok and other sources and wants no platform lock-in.
8. **Disable the Vben notification widget by default** (2026-05-20). In Vben admin panels the top-bar notification bell shows only mock/demo data and has no real backend. Set `widget.notification: false` in `@core/preferences` config unless the project explicitly wires a real notification feed.

## 5. Output Format Preferences

### Comparison Tables (MANDATORY)
Whenever the user requests a "comparison table" / "before and after" / "compare X vs Y", the table MUST include:
- **Before** vs **After** per item
- **Token cost** (before/after + delta)
- **Speed** (before/after)
- **% speed increase** (numeric)
- **Rating 1/10** (before/after)

Estimates are acceptable when exact numbers aren't measurable — flag uncertainty, never skip a column. Applies even when the user doesn't explicitly list the metrics. Full rule in `AGENTS.md` → "Comparison Table Format".

---
*Generated by Antigravity Core — V1.0 User DNA Sync*
